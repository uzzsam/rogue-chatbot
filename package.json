{
  "name": "rogue-chatbot",
  "version": "1.1.0",
  "description": "Edge-streaming Grok chatbot for Wix",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",              // local edge function testing
    "deploy": "vercel deploy --prod"  // optional one-liner
  },
  "dependencies": {
    "undici": "^6.16.0"               // fetch polyfill for `vercel dev`
  },
  "engines": {
    "node": "18.x"                    // matches Vercel Edge runtime
  },
  "license": "MIT"
}
